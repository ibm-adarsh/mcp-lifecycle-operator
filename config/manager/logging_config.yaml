apiVersion: v1
kind: ConfigMap
metadata:
  name: config
  labels:
    app.kubernetes.io/name: mcp-lifecycle-operator
    app.kubernetes.io/managed-by: kustomize
data:
  # Valid values match --zap-log-level: debug, info, error, panic, or an integer > 0.
  log-level: info
